config:
  target: "http://0.0.0.0:8080"
  http:
    pool: 10
    timeout: 10
  phases:
    - duration: 300
      arrivalRate: 300
      name: Heavy load

scenarios:
  - flow:
    - get:
        url: "/movies"
